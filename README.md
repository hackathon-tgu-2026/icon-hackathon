# Resume & Job Matcher Research 🚀

Репозиторий содержит исследования алгоритмов сопоставления резюме с вакансиями (Job Matching) и интерактивного Telegram-бота для демонстрации результатов.

## 📂 Структура проекта


* olga_section/: Исследование решения с использованием модели "nomic-ai/nomic-embed-text-v1.5". (Автор: Садовская Ольга)

* research_v2/: Исследование решения с использованием модели "all-MiniLM-L6-v2" и KeyBERT для извлечения скиллов. (Автор: Тайкина Виктория)

* tg_bot/: Телеграм-бот для демонстрации решений. (Автор: Колесникова Елена)

## 🛠 Описание Telegram-бота

Телеграм-бот выступает в роли интерфейса между пользователем и моделями сопоставления (Matching Models).

### Режимы работы

Бот поддерживает два ключевых сценария взаимодействия:

* 🔍 Поиск вакансий (Find vacancies): загрузите файл с резюме или введите краткое описание в одно сообщение, и система предложит список наиболее подходящих вакансий из базы вакансий.

* ⚖️ Проверка соответствия (Show match): выберите вакансию из списка, отправьте конкретное резюме или введите краткое описание в одно сообщение для получения детальной оценки релевантности.

### Техническая реализация

* *Обработка документов:* Интегрирован парсер для работы с форматом .docx. При загрузке файла через Telegram API бот автоматически извлекает текстовый слой, очищает его от лишних символов и форматирует для передачи в модель.

* *Интеграция с моделями:* Извлеченный текст передается в соотвествующий сервис системы (Research Core), где происходит расчет векторов и вычисление метрик сходства (Cosine Similarity / BERT Score).

* *Управление состоянием (FSM):* Для реализации сложных цепочек диалогов (например, последовательная загрузка сначала вакансии, а потом резюме) используется конечный автомат состояний (Finite State Machine), и отдельное хранение состояния для каждого пользователя.

## 🤖 Запуск Telegram-бота

Бот позволяет загрузить резюме (.docx) и увидеть, как каждое из разработанных решений оценивает их соответствие вакансиям.

### 1. Подготовка
Убедитесь, что у вас установлен Python 3.9+ и создано виртуальное окружение.

### 2. Установка зависимостей

Перейдите в папку бота и установите необходимые пакеты:

```bash
cd tg_bot
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Для работы бота необходимо передать API-токен.

Linux/macOS:

```bash
export iconi_bot_token='ВАШ_ТОКЕН_ЗДЕСЬ'
```


Windows (PowerShell):

```powershell
$env:iconi_bot_token="ВАШ_ТОКЕН_ЗДЕСЬ"
```

### 4. Запуск

```bash
python main.py
```

## 🛠 Технологии

Python, python-docx

NLP: Scikit-learn, sentence_transformers, keybert.

Bot: Telebot.

Data: Numpy.
